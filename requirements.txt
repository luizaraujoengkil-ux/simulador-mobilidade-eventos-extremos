# ============================================================================
# SIMCIV-Mob — dependências (deploy Streamlit Community Cloud)
# Versão LEVE e robusta: sem osmnx/geopandas (build garantido).
# O app usa a rede sintética + importação de GeoJSON. Para ativar a malha
# REAL do OpenStreetMap, ver requirements-osm.txt e o README.
# Recomendado: Python 3.12 (Advanced settings no deploy).
# ============================================================================

# Núcleo da aplicação
streamlit>=1.40
pandas>=2.0
numpy>=1.26
networkx>=3.2

# Mapas interativos
folium>=0.16
streamlit-folium>=0.20
branca>=0.7

# Gráficos
plotly>=5.20

# Relatório e manual em PDF
xhtml2pdf>=0.2.13
